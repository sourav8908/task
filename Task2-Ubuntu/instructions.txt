•	Pull the Ubuntu image.
•	Run it in an interactive container named ubuntu-test.
•	Inside the container, execute:
o	cat /etc/os-release
o	apt update
•	Exit the container and verify it appears in docker ps -a.
•	Restart and attach to the same container again.
•	Stop and remove the container.
✅ Objective: Validate container lifecycle and interaction.
