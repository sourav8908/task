Assignment Instructions

Pull the official MySQL image from Docker Hub.

Run it in a detached container named mysql-test.

Expose port 3306 to the host system.

Use the following environment variable while running the container:
MYSQL_ROOT_PASSWORD = my-secret-pw

Verify it’s running using docker ps.

View container logs to confirm MySQL started correctly.

Connect to the container using the corresponding command.

Enter the MySQL root password when prompted.

Create a database named devops using the corresponding command.

Check the database using the corresponding command.

Exit the MySQL shell using EXIT.

Stop the container (do not remove it).